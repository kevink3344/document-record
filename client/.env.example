# -------------------------------------------------------
# Required for production build (Vite)
# -------------------------------------------------------

# Full URL to the backend API (no trailing slash).
# On Azure, if the client is served by the same App Service as the server,
# use a relative path: /api
# If they are separate services, use the full URL.
VITE_API_BASE_URL=/api
